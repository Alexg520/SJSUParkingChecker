plugins {
    id 'java'
}

group 'org.alex'
version '1.0'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

jar {
    manifest {
        attributes(
                'Main-Class': 'Main'
        )
    }
}